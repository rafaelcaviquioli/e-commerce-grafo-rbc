{% extends 'base.html.twig' %}

{% block body %}
    <h1>ProdutoVisita</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produtoVisitum.id }}</td>
            </tr>
            <tr>
                <th>Datacadastro</th>
                <td>{{ produtoVisitum.datacadastro }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('produtovisita_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('produtovisita_edit', { 'id': produtoVisitum.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
