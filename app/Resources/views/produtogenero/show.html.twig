{% extends 'base.html.twig' %}

{% block body %}
    <h1>ProdutoGenero</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produtoGenero.id }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ produtoGenero.descricao }}</td>
            </tr>
            <tr>
                <th>Peso</th>
                <td>{{ produtoGenero.peso }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('produtogenero_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('produtogenero_edit', { 'id': produtoGenero.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
