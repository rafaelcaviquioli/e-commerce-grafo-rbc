{% extends 'base.html.twig' %}

{% block body %}
    <h1>ProdutoCategoria</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produtoCategorium.id }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ produtoCategorium.descricao }}</td>
            </tr>
            <tr>
                <th>Peso</th>
                <td>{{ produtoCategorium.peso }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('produtocategoria_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('produtocategoria_edit', { 'id': produtoCategorium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
